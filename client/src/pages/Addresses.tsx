import { PageWrapper } from "@/components";
import { EmptySectionContent } from "@/modules";

export const Addresses = () => {
  return (
    <PageWrapper>
      <EmptySectionContent section="addresses" />
    </PageWrapper>
  );
};
