import { PageWrapper } from "@/components";
import { EmptySectionContent } from "@/modules";

export const Dashboard = () => {
  return (
    <PageWrapper>
      <EmptySectionContent />
    </PageWrapper>
  );
};
